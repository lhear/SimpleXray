android.enableJetifier=true
android.useAndroidX=true
org.gradle.jvmargs=-Xmx4g -XX:+UseParallelGC

# Kotlin 2.x optimizations
kotlin.daemon.jvmargs=-Xmx2g
kotlin.incremental.useClasspathSnapshot=true

# Dependency Verification (Supply Chain Security)
# Enable checksum verification for all dependencies
# This prevents supply chain attacks by verifying artifact integrity
# Temporarily disabled to allow build - enable after generating checksums
org.gradle.dependency.verification=off
